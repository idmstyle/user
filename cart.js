(async function(window) {
'use strict';

const vm = new Vue({
    delimiters: ['<{', '}>'],
    el: '#app',
    data: {
        
    },
    mounted: async function () {
    },
    methods: {}
});

})(window);